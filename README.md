# Hytale Player Model setter

Lets players set their model and remember it between sessions

## Installation

Drag and drop jar file into mods/

## Usage

Use `/mymodel Crocodile` in game to set your model to a crocodile, permanently! Works better if users create custom characters for themselves.
